<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="applicationname.companydomain.finalproject1">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera2.full" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launchicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PictureActivity"
            android:theme="@style/AppTheme.NoTitleActionBar">
        </activity>
        <activity android:name=".NutrientsActivity">
        </activity>
        <activity android:name=".PractiveCamera2Activity"
            android:theme="@style/AppTheme.NoTitleActionBar">
        </activity>
        <activity android:name=".UserInformationActivity"
            android:theme="@style/AppTheme.NoTitleActionBar">
        </activity>
        <activity android:name=".UserNameActivity"
            android:theme="@style/AppTheme.NoTitleActionBar" >
        </activity>
        <activity android:name=".SearchFoodActivity"
            android:theme="@style/AppTheme.NoTitleActionBar">
        </activity>
        <activity android:name=".UserFoodInfoActivity">
        </activity>
    </application>
</manifest>